import React from 'react';
import { Typography } from '@material-ui/core';
import { observer } from 'mobx-react-lite';


const Portfolio = observer(() => {
    return(
        <Typography>Badger Portfolio Placeholder</Typography>
    )
})

export default Portfolio;